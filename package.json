{
  "name": "ksef2gdrive",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "smoke": "bash smoke/run.sh"
  }
}
